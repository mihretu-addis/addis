# Host port for nginx (3030 on host → 3000 in container).
APP_PORT=3030

# Set automatically by CI on each deploy.
APP_IMAGE=ghcr.io/your-org/addis-tach-main:latest

# Optional build-time URLs (baked into the image at CI build).
NEXT_PUBLIC_SITE_URL=https://addistech.com
NEXT_PUBLIC_API_URL=
